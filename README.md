# Registered Cars test application

To Run the project follow the instruction below
clone the repository
install node modules (run npm i in project directory)
npm start (To start the development server)
